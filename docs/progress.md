# おさらいノート 進捗

最終更新：2026-08-25

この文書は現在地だけを簡潔に管理する。将来仕様は [study-support-plan.md](./study-support-plan.md)、判断理由は [adr/](./adr/) を参照する。

## 現在の段階

漢字MVPと問題生成基盤、文化庁語例からの846件の下書き作成まで完了。3年生は通常下書きと特別バッチのレビューを終え、未確認・未作成素材が0件になった。現在は4年生第1バッチまで反映済み。

## 完了

- React、TypeScript、Viteのアプリ基盤
- Xiaomi Pad 6向け横向き固定レイアウト
- 家庭学習用ホーム画面
- ひらがな50音表による読み回答
- 濁点、半濁点、小文字、削除、再回答
- Hanzi Writerによる一画ごとの書き順判定
- 複数字を一字ずつ書く進行
- 「分からない」のミス記録、答え・書き順表示、再練習
- 3年生200字、4年生202字の履修設定
- IndexedDBの回答履歴、履修状態、設定、カスタム問題用ストア
- 回答IDの冪等保存と禁止データの拒否
- バージョン付き問題パックの読み込みと検証
- Web App Manifest、Service Worker、GitHub Pages用Actions
- GitHub PagesをActions方式で公開し、HTML、JavaScript、CSSのHTTPS配信を確認
- 家庭内LANからXiaomi Pad 6で表示・手書き感度を確認
- Git初期化と初回コミット
- プロジェクト表示名を「おさらいノート」、npm名を `osarai-note` へ変更
- IndexedDB v1からv2への非破壊移行
- 当日セッションの冪等作成・取得
- 回答、セッション進捗、漢字別集計の原子的保存
- 読みの連帯責任、書きの文字別責任、苦手度0〜10の保存層
- 順番外・競合ID・再試行時のロールバックと二重計上防止
- 練習回数の少ない漢字を優先し、同数を開始ごとの乱数で並べる当日問題の選定
- セット内の対象漢字重複回避と、候補不足時の補充
- 読み・書きの回答中セット作成と、開始ごとの新規抽出
- 読み・書き画面の原子的な回答・進捗保存への接続
- ホームの読み・書き別進捗、完了結果、もう10問
- 文化庁の版付き音訓基準一覧（3年494読み、4年435読み）
- 共通素材スキーマと読み・書きペア生成スクリプト
- 表示語と手書き対象漢字の分離、旧問題パックの読み込み互換
- 公開済み問題IDの維持による当日セット・回答履歴との互換
- 重複、音訓基準、学年配当、ひらがな回答の機械検査
- `approved` だけを公開するレビュー一覧と929読みのカバレッジ表
- 文化庁語例929件分の語句候補作成と、学年配当による自動選定
- 学年内表記で利用可能な868候補の読み付与
- 多義語16件、書き換え52件、語例なし9件の個別確認キュー
- 読み照合済み846件を、公開されない `draft` 共通素材として追加
- 元データ指紋と状態遷移検査を持つ20〜50件単位のレビューバッチ
- 3年生第1バッチ20件の自然な例文を承認し、読み・書き40問を追加
- 承認済み書き問題25字の日本語ストロークデータ自動収録と欠落検査
- 書き問題にも、答えをひらがなに置き換えた読み問題と同じ例文を表示
- 書き問題のかぎ括弧を漢字部分の読みに限定し、送り仮名を分離
- 更新直後に古い問題データが画面へ残っていても、漢字部分の読みを復元して空欄を防止
- 手書きガイド枠、入力領域、文字SVGを固定470×470pxへ統一
- 全問題から読み／書きを直接選ぶ自由練習と、未履修漢字の二段階除外
- 自由練習の回答履歴・苦手度への原子的反映と、今日のセットからの分離
- 自由練習の固定された学年・開始操作、全漢字の縦スクロール一覧、漢字別の読み・書き苦手度グラフ
- 学年からランダム最大10問、漢字タップから関連最大10問を問題選択なしで開始する自由練習セット
- 自由練習の回答画面タブで同じ問題ペアの読み・書きを切り替え
- 「今日の漢字」を押すたび、現在の練習回数が少ない順・同数ランダムで新しい最大10問を抽出
- 未完了セットを再開せず、ホームには当日の確定回答数を読み・書き別に累計表示
- 読み問題も漢字部分だけを強調・回答判定し、送り仮名を例文に残す表示へ統一
- 読みの「分からない」で1ミス・未知を記録し、答え確認後に正解入力を必須とする導線
- 回答画面ヘッダーから設定導線を除外し、ホームと自由練習一覧への導線だけを表示
- 3年生第2・第3バッチ各20件、第4バッチ49件を承認し、自然な例文で公開
- 第4バッチ「係る（かかる）」を使用頻度が低い問題として `needs-fix` へ移し、公開対象から除外
- バッチJSONから問題一覧、全部OK、個別修正、判定送信を生成する確認画面スクリプト
- 3年生第5バッチ100件を承認し、自然な例文で公開
- 3年生第6バッチ100件を承認し、自然な例文で公開
- 3年生第7バッチ100件を承認し、自然な例文で公開
- 3年生第8バッチ25件を承認し、3年生の通常下書きレビューを完了
- 4年生第1バッチ99件を承認し、「群すずめ（むらすずめ）」1件を使用頻度が低い問題として `needs-fix` へ移行
- 3年生「丁字路（ていじろ）」を古い単語で使用頻度が低い問題として `needs-fix` へ移し、公開対象から除外
- 3年生の候補化可能14件、書き換え候補40件、語例なし候補3件を特別バッチ `kanji-g3-009` で確認し、24件を承認、3年次には不適な33件を `needs-fix` へ移行
- 3年生の「一丁」を「三丁目」、「地区」を「区切り」へ、問題IDを維持して差し替え
- ADR-0006に基づき、教科横断ポイントとペット育成のデータ層（DB v3 `motivation`ストア、既存トランザクションへの1問1ポイント加算、`feedPet`によるエサやり・成長・ペット切り替え）とホーム常駐UI（`PetWidget`）を追加。ペットの絵は本物の段階別イラストが未着手のため、絵文字とCSSアニメーションによる仮表示
- がんばり記録画面（`Achievements`）を追加。学習日数、できるようになった漢字（読みまたは書きの苦手度0）、もう少しの漢字（苦手度5以上）、育て終えたペット一覧を表示する。ホーム画面ヘッダーから遷移できる
- ADR-0007に基づく複数教科対応の土台。`validateManifest`が`kanji`以外のパックを例外にしていた問題を解消し、当日セッションへ`subject`を追加（無いレコードは読み出し時に`kanji`扱い、索引は`[localDate, mode]`のまま据え置き）、セッションIDを`教科`込みに変更、ホームの当日集計を`kanji`で明示的に絞るようにした
- 単位のドメインモデル（`src/units.ts`）。5系統19単位の換算係数と学年配当、整数演算による入力解析・表示。DB v4の`unitStates`ストア（`系統:出題形式`キー）と`recordUnitSessionAttempt`（回答履歴・進捗・集計・1ポイントを同一トランザクションで確定）
- 単位の問題形式と検証（`src/unitContent.ts`）、機械生成スクリプト（`scripts/generate-unit-content.mjs`、`npm run content:generate-units`）。単位変換116問・大小比較60問の計176問を`public/content/units-v1.json`へ生成し、マニフェストへ追加した
- 単位の出題器（`src/unitSession.ts`）。`系統:出題形式`の練習回数が少ない順・同数はセットごとの乱数で最大10問を抽出し、同じ集計キーが偏らないよう散らす。未履修は`系統:学年`（例`area:4`）の組で除外し、既存の`settings`ストアへ保存する（DB版の変更なし）
- 単位の人が確認する3形式の素材（`content-source/unit-materials.json`、適切な単位15件・量感12件・文章題12件）と、素材から問題を組み立てる共有ライブラリ（`scripts/unit-content-lib.mjs`）、確認画面生成（`scripts/render-unit-review.mjs`、`npm run content:review-units`）。全件`draft`のため公開パックにはまだ入っていない
- がんばり記録へ単位の苦手度一覧、ホームの当日進捗へ単位の回答数を追加し、ADR-0007のチェックリスト（教科追加時にがんばり記録とホームへ反映する）を単位について完了させた
- 単位の回答画面（`src/UnitPractice.tsx`、`src/NumberPad.tsx`、`src/unitQuizModel.ts`）。アプリ内テンキーと選択タップ、正解まで再回答、「分からない」は1ミスとして記録し答えを隠して自分で正解するまで先へ進めない。ホームの「単位」カードから開始できる

## 次に実装する

### M2：問題生成

1. 残り311件の4年生下書きを、読み、送り仮名、自然さ、意味の順で最大100件ずつレビューする
2. 4年生の未作成24件を個別に処理する
3. 確認済み素材を段階的に公開問題へ追加する
4. 全402字の日本字形・書き順データを確認する

## 既知の未実装・制限

- 公開済み標準問題は確認済み558素材から生成した読み558問・書き558問
- 音訓基準929件のうち、311件は未確認の下書き、36件は要修正、24件は素材未作成。3年生の未確認・未作成素材は0件
- バッチ作成直後の例文は中立文であり、確認画面を作る前に自然な文へ改善する必要がある
- カスタム問題の管理画面は未実装
- がんばり記録画面は実装済み。個々の回答履歴を時系列で見る学習履歴画面は未実装
- JSONバックアップ、復元、初期化は未実装
- GitHub Pagesは`.github/workflows/pages.yml`で`push`（main）と`workflow_dispatch`の両方から実行される（2026-08-19変更、以前は手動実行のみ）
- **糸偏の書き順が日本の標準と異なる（2026-08-27に確認、未修正）。** `@jamsch/hanzi-writer-data-jp` の糸偏は下3画を「左→中→右」の順で書くが、KanjiVGが示す日本の標準は「中→左→右」（＝「小」の書き順）である。収録済みの該当字は級・給・終・線・緑・練の6字。単体の「糸」は「中→左→右」で正しく、「必」もKanjiVGと一致するため、データ全体が中国式なのではなく**糸偏に限定した不具合**である。ただし確認したのは抜き取りであり、全368字の書き順監査は未実施
- 全402字の日本字形・書き順レビューは未実施
- ペット育成の絵は仮表示（絵文字＋CSSアニメーション）。段階ごとの専用イラスト（通常・しょんぼり、2種×5段階）の作成が未着手。差し替え先は `src/petPresentation.ts` の1か所
- ペットのエサやり導線は実画面で確認済み（2026-08-26）。**成長段階の見た目変化（100ポイントごと・全5段階）と、しょんぼり表示および1問回答での回復は実画面で未確認**。段階変化には1匹あたり最大500ポイント、しょんぼりには3日の放置が必要で、通常操作では到達できないため。`PetWidget` と `Achievements` のコンポーネント自体に自動テストは無く、検査しているのは `growthStage` と `isPetNeglected` の純関数だけ
- ペットを2種類とも最高段階まで育てた後は「2ひきとも さいだいまで そだてたよ！」の表示で止まる。3種類目以降やその後の体験は決めていない
- JSONバックアップ・復元が未実装のため、ポイント残高・ペット状態のバックアップ対象化（ADR-0006）は実装済みデータ層のみで、バックアップ機能自体の実装時に反映する
- がんばり記録の「そだてた なかま」は最高段階まで育て終えたペットだけを載せる。育成中のペットはホームのみ
- 分数、日本地図、理科は未実装

- ホームと自由練習の画面整理（2026-08-27）：ノート風イラストを削除し、「今日の学習」の文言を漢字と単位の2教科向けに書き直して「今日の単位をはじめる」を追加した。ホームのヘッダーから「おうちの復習」を外し、「未習漢字を設定」は「保護者設定」と同じ遷移先だったため削除した。「問題を選んで自由練習」は漢字カード内の小さな「選んで練習」へ移した。教科カードは入れ子ボタンを避けるため、カードを`div`にして`.subject-main`と`.subject-sub`の2つのボタンを持つ構造へ変えた。
- 単位追加で崩れていたホームの修正（2026-08-27）：`.today-progress div`の`min-width: 128px`が3つ並ぶと404px必要になり、`.today-card`の`1.15fr`カラムを超えてイラスト欄から幅を奪っていた（ノートが138×361pxまで潰れ、文字が1字ずつ改行されていた）。イラスト自体を削除したため解消したが、同じ理由で折り返していた「何を復習する？」と単位カードの説明文も直した。
- ペット表示の強化（2026-08-27）：絵柄を64px→108pxへ拡大し、エサやりをペットの隣へ寄せた。段階内の進捗バー（`role="progressbar"`）を追加し、「つぎのそだちまで あと○」を表示する。最高段階では「さいだいまで そだったよ！」に切り替わる。
- 書き画面のミス表示（2026-08-27）：常時出ていた「ミス 0回」「見本 未使用」を、実際に発生したときだけ`.live-stat-quiet`で控えめに表示する形へ変えた。[study-support-plan.md](./study-support-plan.md)2章の「不正解を責める表現を使わない」に合わせたもので、件数の表示が仕様上必要な完了画面は変更していない。
- 50音キーの空きマス（2026-08-27）：`.kana-grid button:disabled`の枠線と背景を透明にし、押せないマスがキーに見えないようにした。
- がんばり記録の2カラム化（2026-08-27）：横向きの幅を使うため`.achievements-workspace`を2カラムにし、統計行だけ全幅に残した。
- 漢字の読み検索と学年の複数選択（2026-08-27）：自由練習に読みでの検索と、学年のチェックボックス（複数選択可）＋「全学年」ボタン、明示的な「ホームへ戻る」ボタンを追加した。検索は問題データの`primaryKanji`と`canonicalReading`から索引を作る（音読みはカタカナ、訓読みはひらがなで入っているため、カタカナをひらがなへそろえて突き合わせる）。漢字そのものでも引ける。`contentPack.ts`の型へ`primaryKanji`と`canonicalReading`を**任意項目として**追加した（既存パックの検証を壊さないため）。一覧のタイルには読みを表示する。実行で確認した結果は、「あく」→悪・開、「ショク」と「しょく」→どちらも植、「およ」→泳、「悪」→悪、該当なしの語→空表示、空欄→200字。学年は3年のみ200字、4年のみ202字、両方で402字、どちらも外すと「学年を選んでね」。
- 一覧の問題数計算の見直し（2026-08-27）：漢字1字ごとに`createFreePracticeBatch`を呼んでいたため、全学年表示（402字）では毎回`Map`生成とソートが402回走っていた。`countQuestionsPerKanji`で1回の走査にまとめた。ペア単位で数え10問で頭打ちにする既存の意味は変えていない。

- 読み・書きの選択を開始時へ移動（2026-08-27）：練習ヘッダーの読み／書きタブを廃止し、`src/KanjiModeChoice.tsx` の選択画面（「どっちを練習する？」）を開始前に挟む形へ変えた。ヘッダーは現在の形式をラベル表示するだけになる。**練習中に読み↔書きを切り替える機能（同じ問題位置を保ったまま切替）は意図的に廃止した**（利用者の判断）。`switchPracticeMode` と `.practice-mode-switch` は削除済み。選択画面はホームからの「今日の漢字」と、がんばり記録の漢字タップの両方から使う。
- がんばり記録からの演習開始（2026-08-27）：漢字をタップすると選択画面を経てその漢字の関連問題（最大10問）が始まり、たんいをタップするとその単位に絞った10問が始まる。漢字側は`filterFreePracticeQuestions`を通してから組むので、未履修漢字を含む問題は入らない。単位側は`App.tsx`で`unitCategory`により出題プールを絞る。実行で確認した結果は、「練」タップ→書き→訓練など関連4問、「面積」タップ→`area`の問題が連続4問。
- たんいの集計表示を単位ごとへ（2026-08-27）：`category:questionType`（最大25行）で並べていた一覧を、`summarizeUnitCategories`で長さ・重さ・かさ・時間・面積の5行へまとめた。**保存されているキーは変更していない**（表示時に畳んでいるだけ）。苦手度は問題タイプごとの出題数で重み付けした平均とする。まれな問題タイプでの1回のミスが単位全体を「にがて」にしないための重み付けで、テストでも固定している。各行に出題数と初回正解数、`skillLevel`による「とくい／ふつう／にがて」を表示する。
- CSSクラス名の衝突（2026-08-27）：自由練習に追加した読み検索へ`.kanji-search`を使ったが、**`src/KanjiSettings.tsx`が既に同名クラスを使っており**、後方にある既存定義（`width: 120px`）が勝って入力欄が潰れ、プレースホルダーが見切れていた。追加分を`.reading-search`へ改名して解消した。既存の`.kanji-search`は変更していない。
- 単位画面のホームボタン（2026-08-27）：出題中は進捗「1 / 10」がホームボタンと排他になっていて、練習中にホームへ戻れなかった。進捗とホームボタンを併置する形へ変えた。

- 出題への苦手枠の導入（2026-08-27、[ADR-0008](./adr/0008-weak-slot-mixing.md)）：1セット10問のうち**最大4問**を、対象漢字の最大 `weakness` が1以上の問題にあてる。上限であって下限ではなく、該当が無ければ全問がカバレッジ枠になる。並びは苦手度の降順→`lastPresentedAt` の古い順→開始ごとの乱数。単位側も `unitStateKey` に対して同じ規則を適用する。**ADR-0005の「苦手度を並びに使わない」を置き換える変更**であり、既存ADRは書き換えずADR-0008からSupersedeを示した。実データで確認した結果、苦手な4字（作・官・教・畑）を含む問題が1〜4問目に並び、5〜10問目は未出題の漢字になった。カバレッジは遅くなり、読み368字の1周は約25セットから約41セットへ伸びる（意図した対価）。
- 学年選択の教科横断化（2026-08-27、[ADR-0009](./adr/0009-shared-grade-selection.md)）：自由練習の画面内ローカル状態だった学年選択を、`settings` ストアの `id: "grades"` へ保存する単一設定にした。既存の `id` キー方式に乗るのでストア追加もスキーマ版の更新も不要。ホームにチェックボックスを置き、漢字・単位・自由練習が同じ選択を共有する。**未履修設定が優先**で、評価順は「未履修を除外 → 学年で絞る → 苦手枠／カバレッジ枠へ振り分け」に固定した。実行で確認した結果、両方外すと開始ボタンが無効になり「学年を選んでね」を表示し、再読み込み後も保持され、自由練習側にも同じ選択が反映された。
- 苦手度の表示帯の修正（2026-08-27）：一覧のしきい値が「0」と「5以上」しか拾っておらず、**1〜4の帯が丸ごと見えていなかった**。出題器が練習回数の少ない順で選ぶため大半の漢字がこの帯に留まり、がんばり記録の漢字一覧が常に空に近かった。しきい値を3へ下げ、「もう少しの漢字」（苦手度1〜2）と「にがてな漢字」（3以上）に分けた。実データで、従来は空だった「もう少し」に4字が入ることを確認した。
- 検討して採用しなかった案（2026-08-27）：苦手・得意の判定を初回正解率へ変える案は見送った。出題器が練習回数の少ない順で選ぶため、1つの漢字が3回出題されるまでに読みだけで約75セットかかる（368字、1セットで約15字）。標本1つでは率が0%か100%にしかならず、下限を設けると判定が数か月出ない。1回目の遭遇で動く既存の苦手度カウンタの方がこの出題器には合っている。

- ストロークデータをKanjiVGへ移行（2026-08-27、[ADR-0010](./adr/0010-kanjivg-stroke-data.md)）：`@jamsch/hanzi-writer-data-jp` への依存を削除し、KanjiVG（リリース`r20250816`）の368字を `content-source/kanjivg/` へ取り込んだ。`scripts/generate-kanji-character-data.mjs` を書き換え、中心線をarc長で等間隔に標本化して median を作り、その左右へ幅31でオフセットした閉じた輪郭を `strokes` とする。KanjiVGはM/C/c/S/sしか使わないため専用パーサで足りる。1画あたり10標本。**糸偏6字（級・給・終・線・緑・練）の下3画が「左→中→右」から日本の標準「中→左→右」へ直った**ことを生成物で確認した。健全性検査も通っている（strokes/medians数の一致、全輪郭が閉path、座標が範囲内、368字すべて生成）。実アプリの書き画面で「集」を2画ミス0で書けることも確認した。
- 移行に伴うサイズ変化（2026-08-27）：ビルド済みJSが1,251KB→**1,424KB**（+13.8%、gzipでは518→538KB＝+3.9%）。事前見積もり+11%とほぼ一致した。500KB超過警告は従来から出ており、悪化した状態が続いている。
- ライセンスの整備（2026-08-27）：これまで `LICENSE` も出典表記も無く、[study-support-plan.md](./study-support-plan.md)266行の「ライセンスと再配布条件を記録する」が未実施だった。リポジトリのコードを **MIT** とし（`LICENSE`）、第三者の権利表示を `THIRD-PARTY-NOTICES.md` へ集約した。KanjiVGは **CC BY-SA 3.0** で継承条件があるため、`content-source/kanjivg/` と生成物 `src/generated/kanjiCharacterData.ts` は同ライセンスの派生物として扱う。配信物がデータを再配布するため、保護者設定の画面にも出典表示を追加した。
- 未検証（2026-08-27）：**368字すべての見た目は人が確認していない。** 線幅が均一になり明朝系の抑揚が失われるため、全字の表示が変わっている。曲線の多い字が1画10標本で十分滑らかかも未判断で、足りなければ標本数を増やしてサイズと引き換える。

## 最新の検証結果

- `npm test`：166件成功
- `npm run build`：成功
- GitHub Actions：Pagesの初回手動デプロイに成功。ActionランタイムをNode.js 24対応世代へ更新
- 問題生成：3年200字・494読み、4年202字・435読みを抽出し、欠落・重複検査に成功
- 語例候補：868件候補化、846件下書き化、16件読み要確認、52件書き換え、9件語例なし
- レビューバッチ：3年生第1〜9バッチと4年生第1バッチを反映し、558件承認、36件要修正。第9バッチは24件承認・33件要修正で、生成・学年配当検査に成功
- ストロークデータ：承認済み問題に必要な368字を収録し、「暗」の書き問題が実画面で一画ごとの判定開始まで成功
- 書き問題の文脈：新形式では例文を必須化し、「外が『くらい』ので、電気をつけます。」の表示と答え非表示を実画面で確認
- 送り仮名表示：「外が『くら』いので」と「『くら』の部分を漢字で書こう」を実画面で確認
- 更新時互換：空のかぎ括弧が出ていたタブを再読み込みせず「『くら』い」へ復旧できることを確認
- 書き領域：実画面で外枠474×474px、ガイド・入力領域・SVGが470×470pxへ一致することを確認
- 自由練習：3年生200字の全件縦スクロール、上部操作の固定、ランダム10問開始、漢字「暗」から関連2問への直接開始、同じ問題位置での読み→書き切替を実画面で確認
- 自由練習保存：未履修拒否、全更新ロールバック、苦手度反映、再試行の二重計上防止、当日セッション非作成を検査
- 今日の漢字：未完了セットがあっても新規セッションを作り、同頻度候補を開始ごとの乱数で並べることを検査。実画面でも連続開始で異なる問題になることを確認
- 読みの回答範囲：「悪い」で例文全体を保ったまま「悪」だけを強調し、回答を「わる」に限定することを実画面で確認
- 読みの「分からない」：答え表示中は50音入力を止め、答えを隠して再入力へ戻り、未知回数を一度だけ保存することを検査・実画面確認
- 回答ヘッダー：設定ボタン0件、ホーム1件、自由練習時の問題一覧1件を実画面で確認
- 実ブラウザ：ホームで当日確定数を累計表示し、開始→ホーム→再開始で異なる新規問題を表示。新規タブの警告・エラー0件
- Xiaomi Pad 6：家庭内LAN接続と手書き入力感度は良好
- 問題パック：素材版`2026.08.15-4`、読み558問・書き558問、合計1116問
- 確認画面生成：3年生特別バッチ57件の判定送信と反映に成功。次は4年生第2バッチを生成する
- ビルド警告：ストロークデータ増加により生成JavaScriptが500KBを超過。動作は成功しているが、将来は遅延読み込みを検討する
- 書き問題の完了バグ修正（2026-08-19）：最後の1問を書き終えた直後に保存が失敗すると、画面が「もう一度『次へ』を押してね」と表示したまま`character-complete`へ戻り、再度押しても同じ回答IDで保存済み判定に阻まれて無反応になる不具合と、最終問題の完了直後に次問題がまだ無い状態で「次へ」を押すと何も起きない不具合を修正。保存の再試行を専用の`saveWordCompletion`/`retrySaveWord`に分離し、`nextWord`は次問題が無ければ結果画面（もう10問／ホームへ）へ必ず遷移するようにした。`npx tsc --noEmit`・`npm test`（63件成功、既存と同数）・`npm run build`は成功。ただし`src/App.tsx`の書き完了フローに自動テストは無く、今回の分岐追加もテストで担保されていない。実機確認は、IndexedDBへ直接書き込むフォールスルー検証をハーネスの安全分類器が拒否したため、最後の1問まで実際に手書きして到達する検証は未実施（通常の書き取り操作はコンソールエラー無しで動作することのみ確認）。次回作業時に実機で10問完走して確認するとよい。
- モチベーション機能追加（2026-08-25）：DB v3の`motivation`ストア、1問1ポイント加算、`feedPet`、ホームの`PetWidget`を追加し、移行・ポイント加算・エサやり・ペット切り替え・上限到達のテストを`src/storage/indexedDb.test.ts`と`src/storage/schema.test.ts`に追加した。作業環境で`npm install`がレジストリ403のため失敗し`node_modules`が無く、`npm test`・`npm run build`が実行できていない。上記の63件成功という数字は今回の変更前の状態のままであり、次回作業時にnpmレジストリへアクセスできる環境で`npm test`・`npm run build`を実行し、この節を更新すること。
- 型チェックコマンドの注意（2026-08-26）：ルートの`tsconfig.json`は`"files": []`のプロジェクト参照構成のため、**`npx tsc --noEmit`は1ファイルも型チェックせず、常に無言で成功する**。型チェックには`npx tsc -p tsconfig.app.json --noEmit`（または`npm run build`が使う`tsc -b`）を使う。2026-08-25のモチベーション機能追加時の「`npx tsc --noEmit`成功」という記録はこの理由で無意味であり、当該変更は型チェックされていない。次回`npm install`が通る環境で改めて確認すること。
- 単位の数値演算（2026-08-26）：`src/units.ts`の換算・入力解析・表示を`node --experimental-strip-types`で実行し、17件の確認と例外・往復変換が成功した。IEEE 754では`8.2 * 100`が`819.9999999999999`になるため、入力の解析は小数を仮数と桁数へ分けた整数演算で行う。係数が10の累乗でない単位（分・時間）の端数は小数表示できない（90秒を「1.3分」と誤表示しうる）ため`formatBaseValue`が例外を投げる。時間の数量は表示単位で割り切れる値だけを出題する。
- 単位問題の生成（2026-08-26）：176問（単位変換116、大小比較60）を生成し、`validateUnitPack`による全件検証、数値回答の往復（期待値を入力すると正解、+1すると不正解）、比較60問の正解を表示ラベルから独立に再計算する監査がすべて成功した。生成時に見つけて直した3点は、(1)`1cm²は何m²ですか`（答え0.0001）のような小さすぎる答え。上位単位への変換は答え側から組み立てるようにした。(2)`2km²と19999999999cm²`のような桁違いの比較。単位間の比率を10000倍までに制限した（1m²=10000cm²を残すため1000ではなく10000）。(3)**比較問題の正解がすべて左側**で、最初の選択肢を押し続ければ全問正解できた。問題IDのハッシュで正解の位置を決めるようにし、現在は左29問・右31問。
- 単位の人が確認する3形式（2026-08-26）：39件（適切な単位15、量感12、文章題12）を作成し、承認済みとみなした状態での試験生成、`validateUnitPack`による全件検証、文章題の答えと説明の突き合わせを行った。生成器へ**問題文の漢字が学年配当を超えていないかの検査**を追加したところ、「○○をはかる**単位**はどれですか」の「単」「位」がどちらも4年生の配当漢字で、3年生の問題12件が違反していることを検出した。問題文を「どのたんいではかりますか」へ変更した。検査自体が働くことも、違反する文へ戻すと生成が止まることで確認済み。文章題は答えが個数になるもの（何ばい分）と時計の読み取りが児童に不自然だったため2件を書き直した。
- 単位の出題器と回答フロー（2026-08-26）：実際の176問パックに対して出題器を実行し、10問が別々の集計キーへ散ること、同じ種で同順・別の種で別順になること、練習済みキーが後回しになること、未履修の除外、全除外時に空になることを確認した。回答フローとテンキーも実行で確認し、初回正解・誤答後の再回答・「分からない」後に自分で正解するまで進めないこと・2回目の「分からない」を二重に数えないこと・正解が右側の選択問題を検査した。UIコンポーネント自体（`UnitPractice.tsx`）は`node_modules`が無く実行できていない。
- `node_modules`が無い環境での部分的な型チェック（2026-08-26）：`npx tsc -p tsconfig.app.json --noEmit`は動くが、`react`・`vitest`・`vite`の型が無いため、それらを参照するファイル（`.tsx`全部、`*.test.ts`、`src/contentPack.ts`の`import.meta.env`）は解決エラーで埋まる。外部依存を持たない`src/storage/schema.ts`・`src/storage/indexedDb.ts`・`src/dailySession.ts`・`src/quizModel.ts`などは実質的に検証できるため、出力をこれらのファイルへ絞り`TS2307`を除外して確認する。`src/contentPack.ts(197)`の`Property 'env' does not exist on type 'ImportMeta'`は`vite`の型が無いことによる既存のノイズである。
- ローカル環境での持ち越し検証（2026-08-26）：npmレジストリへ接続できる環境で `npm install`・`npm test`・`npm run build` を初めて実行した。**`npm test` は147件成功**（モチベーション機能と単位教科の追加分を含む。以前の63件から増加）。当日セッションIDを `2026-08-14:kanji:reading:1` 形式へ変えた `src/dailySession.test.ts` も成功。`npm run build`（`tsc -b`）成功。
- ファイル名の大文字小文字衝突の修正（2026-08-26）：`src/UnitPractice.tsx`（画面）と `src/unitPractice.ts`（回答ロジック）が大文字小文字だけ異なり、Windowsやmacの大文字小文字を区別しないファイルシステムでは `import { UnitPractice } from "./UnitPractice"` が**ロジック側の `.ts` を解決してしまい**、`TS2305`（`UnitPractice` は存在しない）と `TS1261`/`TS1149`（同名衝突）でビルドが失敗した。ロジック側を `src/unitQuizModel.ts` へ改名して解消した（画面は `XxxPractice.tsx`、ロジックは `<領域>Model.ts` という既存の `ReadingPractice.tsx`／`quizModel.ts` の対に合わせた）。テストも `src/unitQuizModel.test.ts` へ改名した。**この不具合はLinuxでは再現せず、GitHub Pagesのワークフローはubuntu上で動くためCIでは検出できない。** 大文字小文字だけが異なるファイル名を作らないこと。
- 型チェックの結果（2026-08-26）：改名後は `npx tsc -p tsconfig.app.json --noEmit` がエラー0件。リモート作業で懸念されていた `src/NumberPad.tsx`・`src/Achievements.tsx`・`src/PetWidget.tsx`・`src/Home.tsx` の型エラーは無かった。ただし改名前は `src/App.tsx` の `<UnitPractice ... />` が解決できず暗黙の `any` として素通りしていたため、props整合が検査されたのは今回が初めてである。
- 単位の実画面確認（2026-08-26）：ホームの「単位」カードから10問を完走した。テンキー入力（誤答で入力がクリアされ同じ問題に留まる）、選択タップ、「分からない」（答えと解説を表示し、その状態では「次へ」で進めない。「かくして じぶんでこたえる」で隠してから自分で正解すると `せいかい！` と解説が出る）、最後の1問の正解表示、完了画面の内訳（一回で正解／やり直して正解／分からない）と「もう10問」による新しい10問の開始をすべて確認した。コンソールのエラー・警告は0件。
- ポイントとペットの実画面確認（2026-08-26）：10問の回答（29回の確定）で29ポイントを獲得し、ADR-0006の「回答が確定すれば正誤や『分からない』を問わず1ポイント」と一致することを確認した。エサ1・3・5でポイントが29→20へ減り、「ありがとう！」の表示が出ることを確認した。残高0でエサを押しても何も起きず、残高は負にならない。再読み込み後もポイント20と当日の「たんい 10問」が残ることを確認した。
- 漢字の当日セッションの実画面確認（2026-08-26）：当日セッションIDを `2026-08-14:kanji:reading:1` の教科込み形式へ変えた影響を、単位だけでなく漢字側でも確認した。ホームから今日の漢字を開始し、読み1問（「畑作」→「分からない」で答えを確認し、隠してから「はたさく」を入力して正解）を回答した結果、ホームの当日進捗が「読み 1問」へ増え、ポイントが20→22（「分からない」1回＋正解1回＝2回の確定）へ増えた。再読み込み後も保持される。書きの画面も新しいセッションIDで正しく開き（例文表示、読み表示、1文字目の枠、コンソールエラー0件）、教科込みIDへの変更で漢字側が壊れていないことを確認した。**ただし書きの10問完走は、手書きストローク入力を自動化できないため今回も未実施であり、上記2026-08-19の項目に書いた実機確認の宿題は残っている。**
- がんばり記録の実画面確認（2026-08-26）：「たんい」の苦手度一覧に、回答した10カテゴリ（面積のたんい変かん、長さの大小くらべ、時間のたんい変かんなど）がそれぞれ1問として並ぶことを確認した。「そだてた なかま」は育て終えたペットが無いため「まだいないよ」を表示する。
- 実画面確認の注意（2026-08-26）：ブラウザペインを表示していない状態では合成クリックがヒットテストに失敗して届かないため、確認はページ内でクリックイベントを発火させて行った。また、複数のクリックを同期的に連続実行するとReactのバッチ処理で同じ状態を読むため誤った結果に見える。1クリックごとに待機を挟むこと。

機能変更後は、結果と確認日をこの節へ更新する。詳細ログは残さず、失敗や残課題だけを記録する。
